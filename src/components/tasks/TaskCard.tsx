import { CalendarClock, CheckCircle2, Clock3, UserRound } from 'lucide-react';
import type { TarefaResumo } from '@/types/tasks.types';

export function isOverdue(task: TarefaResumo): boolean { return task.status === 'pendente' && new Date(task.prazoAtual).getTime() < Date.now(); }
export function TaskCard({ task, onClick }: { task: TarefaResumo; onClick: () => void }): JSX.Element {
  const overdue = isOverdue(task); const date = new Intl.DateTimeFormat('pt-BR', { dateStyle: 'short', timeStyle: 'short' }).format(new Date(task.prazoAtual));
  return <button type="button" onClick={onClick} className="w-full rounded-xl border gm-border bg-white p-4 text-left shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300 hover:shadow-md"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold gm-text-primary">{task.tipoAtividadeNome}</span>{task.quantidadeReagendamentos > 0 ? <span className="inline-flex items-center gap-1 text-xs font-semibold text-amber-700"><Clock3 className="h-3.5 w-3.5" />{task.quantidadeReagendamentos}</span> : null}</div><h4 className="mt-3 font-bold leading-5 text-slate-950">{task.titulo}</h4><p className="mt-2 text-xs text-slate-500">{task.escopo === 'evento_macro' ? 'Evento macro' : task.turmaCodigo ? `${task.cursoNome} · ${task.turmaCodigo}` : task.cursoNome}</p><div className="mt-4 flex items-center justify-between gap-3 border-t gm-border pt-3"><span className="inline-flex min-w-0 items-center gap-1.5 truncate text-xs font-medium text-slate-600"><UserRound className="h-3.5 w-3.5 shrink-0" />{task.responsavel.nome}</span><span className={`inline-flex shrink-0 items-center gap-1.5 text-xs font-bold ${overdue ? 'text-red-700' : task.status === 'concluida' ? 'text-emerald-700' : 'text-slate-600'}`}>{task.status === 'concluida' ? <CheckCircle2 className="h-3.5 w-3.5" /> : <CalendarClock className="h-3.5 w-3.5" />}{date}</span></div></button>;
}
